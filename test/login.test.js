const Vue = require("vue");
const chai = require("chai");
const Login = require("../src/pages/login/Login.vue")

describe("Login", () => {
    it("Login name", () => {
        // const Login = Vue.extend(Login);
        // const vm = new Login().$mount();
        // expect(vm.$name.to.equal("backendLogin"));
    })
})